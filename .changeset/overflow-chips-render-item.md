---
"@akiga/design-system-app": patch
---

Let `OverflowChips` render custom chips via `renderItem`. Access pills that already
carry an app mark stay ungrouped, so the +n overflow does not wrap a second tinted
pill around them.
