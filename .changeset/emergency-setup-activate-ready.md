---
"@akiga/design-system-app": patch
---

Emergency Access setup checklist: once every required step is done, show an info note under the list with **Activate** — same action as the header, so the reader does not have to leave the card that just told them they are ready.
