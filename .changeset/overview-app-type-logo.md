---
"@akiga/design-system-app": patch
---

The application Overview Connection card shows the vendor logo next to Application type, so the type is recognized the same way it is in the directory list.
