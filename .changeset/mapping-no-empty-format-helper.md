---
"@akiga/design-system-app": patch
---

The date-pattern field no longer shows “Column format check” under an empty input. The info icon still explains the gate; a typed pattern still reads back as e.g. 09/03/2026.
