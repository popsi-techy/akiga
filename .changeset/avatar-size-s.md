---
"@akiga/design-system-app": minor
---

Avatar gains **`s` — 28px**, the step between xs (24) and sm (32).

It is the table mark: IdentityCell uses it, and Directory EntityAvatar's compact size now maps to it
so a letter avatar and a 28px app logo in the same row are the same box. The person ring keeps the
2px surface gap.
