---
"@akiga/design-system-app": patch
---

Application detail defaults to Reconciliation when the requested tab is not in the strip (drafts hide Overview; Configure is absent when lifecycle provisioning is off).
