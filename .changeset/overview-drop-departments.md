---
"@akiga/design-system-app": patch
---

The application Overview Governance card no longer shows Departments. Scope is not assigned on the application, so the dash was a hole the page could not close.
