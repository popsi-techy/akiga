---
"@akiga/design-system-app": minor
---

Emergency Access V2 preview is one Card per wizard step (same names as the rail), each with an Edit that returns to that step. The header Edit is gone — it only ever opened the first step.
