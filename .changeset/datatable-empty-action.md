---
"@akiga/design-system-app": patch
---

DataTable empty states can take a centered `emptyAction`. Authorization uses it for
Add Authorization when no method exists yet, and hides the toolbar button so the
CTA is not in two places.
