---
"@akiga/design-system-app": patch
---

Reconciliation without a connector now speaks in uploads: last upload / never uploaded, upload history, and a Source column that reads CSV — not last sync / never run / Sync Type.
