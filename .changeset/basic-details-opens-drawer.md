---
"@akiga/design-system-app": patch
---

Basic Details on a catalogued application opens the same drawer as an onboarded one. The click used to toast because only the onboard store could be edited; catalog edits now overlay the seed the same way lifecycle already does.
