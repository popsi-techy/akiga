---
"@akiga/design-system-app": patch
---

The Action column on a closed external identity is a dash, not a Disabled/Terminated chip. Identity status already carries that state; a pill in Action looked like a control when there is nothing to do.
