---
"@akiga/design-system-app": minor
---

Add ModeBar and a Drawer subheader slot so a method that replaces the form stays pinned while that form scrolls. Add authorization uses it for Basic / OAuth.
