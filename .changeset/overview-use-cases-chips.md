---
"@akiga/design-system-app": patch
---

The application Overview Connection card names Use cases instead of Provisioning, shows Discovered via as the same chip as the applications table, and Default baseline is the name only.
