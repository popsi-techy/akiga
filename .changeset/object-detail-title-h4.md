---
"@akiga/design-system-app": minor
---

Object-detail page titles use `text-h4` (18/26) instead of `text-h3` (20/28). The size already existed on the scale — this reassigns the role so detail headers stay on the token and stop overriding line-height with `leading-tight`.
