---
"@akiga/design-system-app": patch
---

The Configure tab is the connector, so it only appears when Enable Provisioning is on. Off means IGA will not push access, and there is no authorization or event work to finish before Connect — the required checklist step is omitted too.
