---
"@akiga/design-system-app": patch
---

Show when an Emergency Access profile last changed.

The list gets a Modified column — clock plus date and time. The tooltip names
it “Last modified”. Writes stamp `updatedOn` so the date moves when the
profile does. Overview Timeline remains the place to read it on the detail.
