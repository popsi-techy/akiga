---
"@akiga/design-system-app": patch
---

Add Emergency Access V3: setup happens on the real tabs, guided by a floating bar (Next / Back / Activate) that disappears once the access is live. The Setup tab is gone. `SetupBar` is the Design System strip that other draft objects can reuse.
