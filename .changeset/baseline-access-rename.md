---
"@akiga/design-system-app": patch
---

Rename the Baseline Governance tab to Baseline Access, on the application detail strip and in the draft setup checklist. The tab lists reference sets of access; "governance" named the department rather than the thing on the page, and every neighbouring tab names its contents. `BaselineGovernanceTab` follows to `BaselineAccessTab`, so the component and the tab cannot drift.
