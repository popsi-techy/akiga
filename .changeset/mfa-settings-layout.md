---
"@akiga/design-system-app": patch
---

MFA settings uses a page title, section saves, and Emergency Access-style grey setting wells with descriptions. The page caps at 900px and shrinks with the viewport. Allowed methods use OverflowChips: a grey pill in a white well, with +n for the rest.
