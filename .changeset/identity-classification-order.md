---
"@akiga/design-system-app": patch
---

Mixed identity classification now reads in evaluation order under one heading: Classify using covers the source field and the rules on a single row, then Fallback type with a hint for when nothing matches. The section intro is gone; the prompt names identity types, and each radio description is seven words.
