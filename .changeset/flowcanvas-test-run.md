---
"@akiga/design-system-app": minor
---

FlowCanvas gains a `simulation` mode for Approval Policy Test Run: dimmed base edges, node status rings, and an animated success/danger trace (respects reduced motion). The builder adds a dummy Test run control that walks the tree and shows a step results panel.
