---
"@akiga/design-system-app": patch
---

The setup checklist Next prompt now treats a finished Configure job as progress — finishing Authorization recommends Connection (or Attribute mapping), not the parent step. The row you are already on still has no CTA.
