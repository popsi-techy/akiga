---
"@akiga/design-system-app": patch
---

Directory detail no longer leaves a 24px canvas strip under the page — the
docked shell cancels `main`’s bottom padding. Application overview’s
Connection and Governance cards fill the remaining height.
