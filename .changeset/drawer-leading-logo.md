---
"@akiga/design-system-app": patch
---

Drawer accepts a `leading` slot for marks that should not sit in the brand icon tile (e.g. an application logo). The onboard-application drawer uses it with a shorter “Onboard {type} Application” header.
