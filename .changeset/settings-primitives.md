---
"@akiga/design-system-app": patch
---

Promote tenant settings anatomy into the Design System (SettingsPage, SettingsSection, SettingsRow, SettingsStack) so new admin screens assemble the same way as MFA.
