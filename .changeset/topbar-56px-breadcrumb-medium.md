---
"@akiga/design-system-app": minor
---

App chrome top bar is now 56px (`layout.topbarHeight`), consumed via `--ds-layout-topbarHeight`. Product breadcrumbs use `text-body-medium` (500) instead of `font-emphasis` (600).
