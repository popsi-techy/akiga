---
"@akiga/design-system-app": patch
---

The setup checklist lists Configure’s own jobs as their own rows under the parent — Authorization, Connection, Attribute mapping, Manage connections — each with a tick, joined by a curved hairline.

Clicking one opens that Configure section. Configure is a group label — no check. The selected hairline is on the job you are in, not around the whole tree.
