---
"@akiga/design-system-app": patch
---

InfoRow values use `text-body-sm-medium` instead of strong, so a detail rail's answers sit at the same weight as other 13px labels rather than shouting next to them.
