---
"@akiga/design-system-app": patch
---

Workflow templates open a blank canvas (event only, no pre-built tree). The builder palette is back to the original per-event set: Joiner full, Mover filter + assign, Leaver notification.
