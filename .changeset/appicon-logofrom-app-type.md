---
"@akiga/design-system-app": patch
---

AppIcon can resolve a vendor logo from `logoFrom` (the app type) when the instance name is custom.

Directory application avatars now use AppIcon, so an onboarded SAP or Adobe tenant named “Finance Prod” still shows the brand mark instead of a letter tile.
