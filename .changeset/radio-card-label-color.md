---
"@akiga/design-system-app": patch
---

RadioCardGroup: keep option labels on primary text when selected, place the radio control before the label, remove card borders, and add an `inline` layout for compact horizontal options.
