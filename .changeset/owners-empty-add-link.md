---
"@akiga/design-system-app": patch
---

An application with no owners now shows a "+ Add owner" text link in the Owners column instead of a dash. The dash said the cell was empty; the link is the next step — it opens that application's Owners tab, where the empty page already has the Add control.
