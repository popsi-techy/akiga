---
"@akiga/design-system-app": minor
---

Avatar: soft corner radius is now 6px (`radius.avatar`, aligned with `radius.sm`). Letter scale is xs 12 / sm 16 / md 20 / lg 24 so a single glyph reads as the mark.
