---
"@akiga/design-system-app": patch
---

Menu accepts an optional spacing offset so the list can sit clear of nearby chrome. The Access Certification header dock uses it so the overflow menu does not cover the pill.
