---
"@akiga/design-system-app": patch
---

Select accepts `hint`, same as Input: an info icon on the label for the sentence you only need once, so a Select no longer has to carry that copy as permanent helper text under the field.
