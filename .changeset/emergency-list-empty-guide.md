---
"@akiga/design-system-app": minor
---

When the Emergency Access list has no profiles, the empty table is replaced by a first-run
state: a key tile, **Add emergency access**, a short description, and two actions — create,
or **View guide**. The guide is a Modal that walks the same setup steps as the draft
checklist, so the reader sees the work before a profile exists.
