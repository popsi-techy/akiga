---
"@akiga/design-system-app": patch
---

Request Governance list: show the current stage as a StatusChip —
`Stage 2: Policy`, or the waiting hop (`Stage 3: Owner Approval`) when
approval is the live stage.
