---
"@akiga/design-system-app": patch
---

Seed an active Joiner onboarding workflow so the catalog is not empty on a first visit. Execution history already narrated three of its runs — one in flight, one completed, one failed — and the tree now uses the same node ids, so the canvas and the history are one story.
