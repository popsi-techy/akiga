'use client';

import { SchedulesScreen } from '@/components/product/reports';

/** `/iga/reports/schedules` — cadences that produce sealed packages. */
export default function ReportSchedulesPage() {
  return <SchedulesScreen />;
}
