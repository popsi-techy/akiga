'use client';

import { ReportEditor } from '@/components/product/analytics/v2/ReportEditor';

/** Create a report from scratch — the shared editor in its "new" mode. */
export default function GovernanceAnalyticsV2CreatePage() {
  return <ReportEditor />;
}
