'use client';

import { EmailTypesListPage } from '@/components/product/settings/email';

/** Email settings — the notification emails this tenant has composed. */
export default function EmailSettingsPage() {
  return <EmailTypesListPage />;
}
