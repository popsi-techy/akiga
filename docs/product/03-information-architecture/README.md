# 03 · Information Architecture

How the product is organized and navigated — the structural map, independent of visual design.

> **Populated** (consolidated). · **Feeds:** `navigation.json`

| Document | Purpose |
|----------|---------|
| `navigation.md` | **The complete IA**: navigation model & groups, permission-aware visibility, module→destination map, hierarchy/depth rules, and routing convention. Navigation, module-map, and hierarchy are kept in one file because they describe the same structure. Mirrored in `navigation.json`. |

> IA here is *conceptual structure*, not layout. Sidebar vs. topbar, spacing, and components
> are Design System decisions, not PKB decisions.
